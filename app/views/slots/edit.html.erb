<%= simple_form_for @slot do |f| %>
    <%= f.input :start_time %>
    <%= f.input :end_time %>

    <div class="d-flex gap-3">
    <%= f.button :submit, class: "btn btn-primary" %>
    <%= link_to "Back to slots", slots_path, class: "btn btn-secondary" %>
    </div>
<% end %>
