<h1>Mes Reviews</h1>
<ul>
  <% @reviews.each do |review| %>
    <li>
    <%= @user = User.find(review.user_id) %>
      <%= link_to @user.email, enterprise_review_path(review) %>
    </li>
  <% end %>
</ul>
