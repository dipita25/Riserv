<table class="table table-bordered">
  <thead>
    <tr>
      <th>Date de début</th>
      <th>Date de fin</th>
      <th>Service</th>
      <!-- Ajoutez d'autres en-têtes de colonne au besoin -->
    </tr>
  </thead>
  <tbody>
    <% @reservations.each do |reservation| %>
      <tr>
        <td><%= link_to reservation.slot.start_time.strftime("%d %B %Y - %H:%M:%S"), enterprise_reservation_path(@enterprise_id, reservation) %></td>
        <td><%= link_to reservation.slot.end_time.strftime("%d %B %Y - %H:%M:%S"), enterprise_reservation_path(@enterprise_id, reservation) %></td>
        <td><%= link_to reservation.service.title, enterprise_reservation_path(@enterprise_id, reservation) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
