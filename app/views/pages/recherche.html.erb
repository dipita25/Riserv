<div class="container">
  <h1>Résultats de recherche pour "  <%= @text %>"</h1>

  <div class="row">
    <div class="cards">
      <% @enterprises.each do |enterprise| %>

        <%= link_to enterprise_services_path(enterprise.id) do %>
          <div class="card-category"
            style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path(enterprise.photo.key, crop: :fill, width: 800, height: 600) %>');">
            <%= enterprise.name %>
          </div>
        <% end %>
      <%end %>
    </div>
  </div>

</div>


<style>
  a {
  text-decoration: none;
}
</style>
