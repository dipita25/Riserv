<!--<h1>Pages#home</h1>

<%= button_to 'entreprises', enterprises_path, method: :get, class: "btn btn-primary" %> <br>
<%= button_to 'Mes réservations', enterprise_reservations_path(@enterprise[0].id), method: :get, class: "btn btn-primary" %> <br>
<%= button_to 'Mes creneaux', enterprise_slots_path(@enterprise[0].id), method: :get, class: "btn btn-primary" %> <br>
<%= button_to 'Mes Services', enterprise_services_path(@enterprise[0].id), method: :get, class: "btn btn-primary" %> <br>
<%= button_to 'Mes Reviews', enterprise_reviews_path(@enterprise[0].id), method: :get, class: "btn btn-primary" %> <br>-->


<div class="container-fluid">
  <div class="row">
    <div class= "col-12 d-flex justify-content-center align-items-center"  style="height: 80vh;">
      <div class="center-div text-center">
        <h1>RISERV</h1>
        <form class="row g-3">
          <div class="col-auto">
            <input type="text" class="form-control" id="searchInput">
          </div>
          <div class="col-auto">
            <%= link_to "recherche", "javascript:void(0)", data: { url: recherche_path(recherche: '') }, onclick: "performSearch()", class: "btn btn-secondary" %>
          </div>
        </form>
      </div>
    </div>
  </div>


</div>



<script>
  function performSearch() {
    // Récupérer l'URL de l'élément attribut data
    var url = document.querySelector("[data-url]").getAttribute("data-url");
    // Récupérer la valeur du champ de texte
    var searchText = document.getElementById('searchInput').value;
    // Modifier l'URL avec la valeur saisie
    var searchUrl = url + encodeURIComponent(searchText);
    // Rediriger vers la nouvelle URL
    window.location.href = searchUrl;
  }
</script>
