<!-- app/views/reservations/show.html.erb -->

<h1>Détails du Service</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Title</th>
      <td><%= @service.title %></td>
    </tr>
    <tr>
      <th>Description</th>
      <td><%= @service.description %></td>
    </tr>
    <tr>
      <th>Price</th>
      <td><%= @service.price %></td> <!-- Supposons que chaque réservation appartient à un utilisateur -->
    </tr>
    <!-- Ajoutez d'autres détails de réservation au besoin -->
  </tbody>
</table>

  <%= button_to 'Supprimer', enterprise_service_path(@service.enterprise_id, @service), method: :delete,
        data: {"turbo_method": :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-primary" %><br/>
<%= link_to "Retour", enterprise_services_path(@service.enterprise_id), class: "btn btn-secondary" %><br/><br/>
<%= link_to "MODIFIER", edit_enterprise_service_path(@service.enterprise_id, @service), class: "btn btn-secondary" %>
